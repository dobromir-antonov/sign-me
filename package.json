{
  "name": "signme",
  "version": "1.0.0",
  "scripts": {
    "build": "shx mkdir -p build && shx cp index.html app.css app.js build/ && shx cp -r assets build/"
  },
  "dependencies": {
    "shx": "^0.4.0"
  }
}